# 更新日志

## 0.1.29 - 2026-07-29

### 新增

- 路由请求日志支持查看详情、复制记录和配置保留天数，默认保留最近 7 天。
- 路由接管同时配置 Codex Provider 与 API Key 认证，并可在确认后自动重启 Codex。
- 路由自检增加 `config.toml`、`auth.json`、服务状态和健康检查诊断。

### 优化

- 重构路由页面布局，消除左侧大面积空白并改善窄窗口显示。
- API Key 默认遮罩，可通过显示按钮临时查看。
- 账号额度、冷却时间、优先级和启用状态统一移入账号编辑弹窗。
- 固定账号模式必须选择有效账号，未选择时阻止保存、启动、测试和接管。
- 路由流式响应现在可识别 `response.failed`、`response.incomplete` 等上游错误事件。

### 修复

- 修复接管配置只写 Provider、未同步 Codex API Key 认证导致请求失败的问题。
- 修复路由日志无法按保留天数自动清理的问题。
- 恢复接管配置时同时恢复原有 Provider 和认证文件，避免覆盖用户登录状态。
