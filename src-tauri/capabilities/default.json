{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Main window permissions for Codex account migration and file selection.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:default"
  ]
}
